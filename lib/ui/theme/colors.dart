import 'package:flutter/material.dart';

class BBVAColors {
  static const Color primaryBlue = Color(
    0xFF00338D,
  ); // Azul BBVA (botones, barras)
  static const Color secondaryBlue = Color(
    0xFF001A4D,
  ); // Azul oscuro (detalles)
  static const Color white = Color(0xFFFFFFFF); // Fondo blanco
  static const Color black = Color(0xFF000000); // Letras negras
  static const Color lightGray = Color(0xFFE0E0E0); // Gris muy claro (bordes)
  static const Color errorRed = Color(0xFFE50000); // Errores
}
